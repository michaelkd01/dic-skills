**BPC Button** — the hard-edged primary action control; use for any commit/CTA. Primary inverts fill↔ground on hover, ghost fades to 60% opacity, all press to 96%.

```jsx
<Button variant="primary" uppercase>Request an introduction</Button>
```

Variants: `primary` (charcoal fill, inverts on hover) · `secondary` (outline, fills on hover) · `ghost` (text-only, opacity hover). Sizes: `sm` · `md` · `lg`. Set `uppercase` for the tracked all-caps CTA label BPC uses on marketing surfaces. Keep labels short and verb-led — never title-case.
